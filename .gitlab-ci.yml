# Configuration for Gitlab-CI.
# Builds appear on https://gitlab.com/buildroot.org/buildroot/pipelines
# The .gitlab-ci.yml file is generated from .gitlab-ci.yml.in.
# It needs to be regenerated every time a defconfig is added, using
# "make .gitlab-ci.yml".

image: maohan001/ubuntu-buildroot

stages:
  - chip-build
  - chip-test
  - qemu-build
  - qemu-test
  - sales-build
  - toolchain-multi-libs

variables:
    PREFIX: ""
    SUFFIX: ""
    CHIP_PREFIX: csky_
    CHIP_SUFFIX: libc_br_defconfig
    QEMU_PREFIX: qemu_csky_
    QEMU_SUFFIX: libc_br_defconfig
    SALES_PREFIX: csky_
    SALES_SUFFIX: _defconfig
    CONFIG_NAME: ""

.build_buildroot: &build_buildroot
    - echo 'Configure Buildroot'
    - CONFIG_NAME=${PREFIX}${CI_BUILD_NAME}${SUFFIX}
    - echo ${CONFIG_NAME}
    - make ${CONFIG_NAME}
    - echo 'Build buildroot'
    - |
        make > >(tee build.log |grep '>>>') 2>&1 || {
            echo 'Failed build last output'
            tail -200 build.log
            xz -z build.log
            exit 1
        }
    - xz -z build.log
    - if [ -f output/images/vmlinux ]; then
          output/host/bin/csky-linux-strip -sg output/images/vmlinux;
          xz -z output/images/vmlinux;
      fi
    - if [ -f output/images/rootfs.cpio ]; then
          rm -f output/images/rootfs.tar;
          xz -z output/images/rootfs.cpio;
      fi
    - if [ -f output/images/rootfs.tar ]; then
          xz -z output/images/rootfs.tar;
      fi

.variety_builds: &variety_builds
    script: *build_buildroot
    dependencies: []
    cache:
      paths:
        - dl/
    artifacts:
        when: always
        expire_in: 2 weeks
        paths:
            - output/images/
            - build.log.xz

.variety_tests: &variety_tests
    - echo 'Prepare to run'
    - mkdir -p output/host/
    - tar -xf output/images/csky_toolchain_*.tar.xz -C output/host
    - if [ -f output/images/vmlinux.xz ]; then
          xz -d output/images/vmlinux.xz;
      fi
    - if [ -f output/images/rootfs.tar.xz ]; then
          xz -d output/images/rootfs.tar.xz;
      fi
    - if [ -f output/images/rootfs.cpio.xz ]; then
          xz -d output/images/rootfs.cpio.xz;
      fi
    - ./output/host/csky-ci/run_test.sh
    - ./output/host/csky-ci/generic_analyze.sh || exit 1

################ chip ################
.chip_build_before: &chip_build_before
    - PREFIX=${CHIP_PREFIX}
    - SUFFIX=${CHIP_SUFFIX}

.chip_build: &chip_build
    stage: chip-build
    before_script: *chip_build_before
    <<: *variety_builds

.chip_test: &chip_test
    stage: chip-test
    script: *variety_tests
    artifacts:
        when: on_success
        paths:
            - test.log

# chip-build
610_gx6622_4.9_uc: *chip_build
610_gx6622_4.16_g: *chip_build
807_eragon_4.9_g: *chip_build

# chip-test
610_gx6622_4.9_uc_test:
    tags:
      - csky-610-gx6622
    dependencies:
      - 610_gx6622_4.9_uc
    <<: *chip_test

610_gx6622_4.16_g_test:
    tags:
      - csky-610-gx6622
    dependencies:
      - 610_gx6622_4.16_g
    <<: *chip_test

807_eragon_4.9_g_test:
    tags:
      - csky-807-eragon
    dependencies:
      - 807_eragon_4.9_g
    <<: *chip_test

################ qemu ################
.qemu_build_before: &qemu_build_before
    - PREFIX=${QEMU_PREFIX}
    - SUFFIX=${QEMU_SUFFIX}

.qemu_build: &qemu_build
    stage: qemu-build
    before_script: *qemu_build_before
    <<: *variety_builds

.qemu_test: &qemu_test
    stage: qemu-test
    script: *variety_tests
    artifacts:
        when: always
        expire_in: 2 weeks
        paths:
            - test.log

# qemu-build
810f_4.16_uc: *qemu_build
810f_4.16_g: *qemu_build
810f_4.9_uc: *qemu_build
810f_4.9_g: *qemu_build
807f_4.18_g: *qemu_build
807f_4.16_uc: *qemu_build
807f_4.16_g: *qemu_build
807f_4.14_uc: *qemu_build
807f_4.14_g: *qemu_build
807f_4.9_uc: *qemu_build
807f_4.9_g: *qemu_build

# qemu-test
810f_4.16_uc_test:
    dependencies:
      - 810f_4.16_uc
    <<: *qemu_test

810f_4.16_g_test:
    dependencies:
      - 810f_4.16_g
    <<: *qemu_test

810f_4.9_uc_test:
    dependencies:
      - 810f_4.9_uc
    <<: *qemu_test

810f_4.9_g_test:
    dependencies:
      - 810f_4.9_g
    <<: *qemu_test

807f_4.18_g_test:
    dependencies:
      - 807f_4.18_g
    <<: *qemu_test

807f_4.16_uc_test:
    dependencies:
      - 807f_4.16_uc
    <<: *qemu_test

807f_4.16_g_test:
    dependencies:
      - 807f_4.16_g
    <<: *qemu_test

807f_4.14_uc_test:
    dependencies:
      - 807f_4.14_uc
    <<: *qemu_test

807f_4.14_g_test:
    dependencies:
      - 807f_4.14_g
    <<: *qemu_test

807f_4.9_uc_test:
    dependencies:
      - 807f_4.9_uc
    <<: *qemu_test

807f_4.9_g_test:
    dependencies:
      - 807f_4.9_g
    <<: *qemu_test

################ sales ################
.sales_build_before: &sales_build_before
    - PREFIX=${SALES_PREFIX}
    - SUFFIX=${SALES_SUFFIX}

.sales_build: &sales_build
    stage: sales-build
    before_script: *sales_build_before
    <<: *variety_builds

# sales-build
860_platform: *sales_build
gx6605s_fbcon: *sales_build
gx6605s: *sales_build

# sales-test
# sales has no test

################ toolchain ################
# toolchain-multi-libs
abiv2-uclibc:
    stage: toolchain-multi-libs
    dependencies: []
    script:
      - echo abiv2-uclibc

abiv2-glibc:
    stage: toolchain-multi-libs
    dependencies: []
    script:
      - echo abiv2-glibc
