diff --git a/drivers/pci/Kconfig b/drivers/pci/Kconfig
index bda1517..62530ca 100644
--- a/drivers/pci/Kconfig
+++ b/drivers/pci/Kconfig
@@ -25,7 +25,7 @@ config PCI_MSI
 	   If you don't know what to do here, say Y.
 
 config PCI_MSI_IRQ_DOMAIN
-	def_bool ARC || ARM || ARM64 || X86
+	def_bool ARC || ARM || ARM64 || X86 || CSKY
 	depends on PCI_MSI
 	select GENERIC_MSI_IRQ_DOMAIN
 
diff --git a/drivers/pci/dwc/Kconfig b/drivers/pci/dwc/Kconfig
index 113e094..ae25b00 100644
--- a/drivers/pci/dwc/Kconfig
+++ b/drivers/pci/dwc/Kconfig
@@ -49,6 +49,41 @@ config PCI_DRA7XX_EP
 
 endif
 
+config PCI_CSKY_ANOLE
+	bool "CSKY Anole PCIe controller"
+	depends on COMPILE_TEST
+	depends on (PCI && PCI_MSI_IRQ_DOMAIN) || PCI_ENDPOINT
+	depends on OF && HAS_IOMEM
+	help
+	 Enables support for the PCIe controller in the DRA7xx SoC. There
+	 are two instances of PCIe controller in DRA7xx. This controller can
+	 work either as EP or RC. In order to enable host-specific features
+	 PCI_DRA7XX_HOST must be selected and in order to enable device-
+	 specific features PCI_DRA7XX_EP must be selected. This uses
+	 the DesignWare core.
+
+if PCI_CSKY_ANOLE
+
+config PCI_CSKY_ANOLE_HOST
+	bool "PCI CSKY Anole Host Mode"
+	depends on PCI
+	depends on PCI_MSI_IRQ_DOMAIN
+	select PCIE_DW_HOST
+	default y
+	help
+	 Enables support for the PCIe controller in the CSKY Anole SoC to work in
+	 host mode.
+
+config PCI_CSKY_ANOLE_EP
+	bool "PCI CSKY Anole Endpoint Mode"
+	depends on PCI_ENDPOINT
+	select PCIE_DW_EP
+	help
+	 Enables support for the PCIe controller in the CSKY Anole SoC to work in
+	 endpoint mode.
+
+endif
+
 config PCIE_DW_PLAT
 	bool "Platform bus based DesignWare PCIe Controller"
 	depends on PCI
