config BR2_PACKAGE_KLITMUS_TEST
	bool "klitmus test"
	depends on BR2_PACKAGE_HOST_HERDTOOLS7
	help
	  The tests can also be used for kernel memroy order test.

comment "litmus test needs herdtools7"
	depends on  !BR2_PACKAGE_HOST_HERDTOOLS7
